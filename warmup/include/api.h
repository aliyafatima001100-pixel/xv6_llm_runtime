/*
 * api.h is the only header students get. It declares the public entry
 * point. What it calls internally (how many helper functions there are,
 * how they call each other, any recursion) is not documented here; that
 * is what the tracing exercise is for.
 */
#ifndef API_H
#define API_H

int compute(int x);

#endif /* API_H */
